const list=document.querySelector('#loc-list'),esc=x=>String(x??'').replaceAll('&','&amp;').replaceAll('<','&lt;');async function load(){let r=await fetch('/api/v1/locations'),d=await r.json();list.innerHTML=!r.ok?`<div class="result-chip">${esc(d.error)}</div>`:d.locations.map(x=>`<div class="result-chip"><strong>${esc(x.name)}</strong><br><small>${esc(x.building)} / ${esc(x.room)} / ${esc(x.shelf)} / ${esc(x.slot)}</small></div>`).join('')||'<div class="result-chip">Zatím bez lokací.</div>'}document.querySelector('#loc-form').onsubmit=async e=>{e.preventDefault();let body={name:document.querySelector("#loc-name").value,building:document.querySelector("#loc-building").value,room:document.querySelector("#loc-room").value,shelf:document.querySelector("#loc-shelf").value,slot:document.querySelector("#loc-slot").value};await fetch('/api/v1/locations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});e.target.reset();load()};load();