const form=document.getElementById('auth-form'),out=document.getElementById('auth-output'),tl=document.getElementById('auth-timeline'),risk=document.getElementById('risk');const e=v=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;');form.addEventListener('submit',async x=>{x.preventDefault();out.innerHTML='<div class="result-chip">Probíhá sandbox simulace…</div>';const body={scenario:scenario.value,attempts:+attempts.value,defense:{rate_limit:+document.getElementById('rate-limit').value,uniform_response:uniform.checked,mfa_required:mfa.checked}};try{const r=await fetch('/api/v1/auth-simulate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await r.json();risk.textContent=d.risk;risk.className='status-badge risk-'+d.risk;out.innerHTML=`<div class="result-chip"><strong>${e(d.title)}</strong><br><small>${e(d.owasp)} · ${e(d.mitigation)}</small></div>`+d.finding.map(i=>`<div class="result-chip"><strong>Nález</strong><br><small>${e(i)}</small></div>`).join('')+`<div class="result-chip"><strong>Bezpečnostní režim</strong><br><small>Sandbox: ${d.sandbox}; externí požadavky: ${d.external_requests}; run: ${e(d.run.id)}</small></div>`;tl.innerHTML=d.timeline.map(i=>`<li><span class="timeline-step">${i.step}</span><div><strong>${e(i.state)}</strong><small>${e(i.reason)}</small></div></li>`).join('')}catch(err){out.innerHTML=`<div class="result-chip"><strong>Chyba</strong><br><small>${e(err.message)}</small></div>`}});