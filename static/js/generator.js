const f=document.getElementById('generator-form'),s=document.getElementById('seed'),o=document.getElementById('generator-output');const chip=(k,v)=>{const d=document.createElement('div');d.className='result-chip';d.innerHTML=`<strong>${k}</strong><br><small>${v}</small>`;return d};f?.addEventListener('submit',async e=>{e.preventDefault();o.innerHTML='<div class="result-chip">generuji…</div>';const r=await fetch('/api/v1/generate-profile',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({seed:s.value.trim()})});const d=await r.json();o.innerHTML='';Object.entries(d).forEach(([k,v])=>o.appendChild(chip(k,v)))});