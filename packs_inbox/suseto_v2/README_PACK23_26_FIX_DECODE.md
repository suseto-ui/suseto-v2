# Pack 23–26 Fix — Decode Lab Output

Opravuje chování Decode Labu, když výstup nic nezobrazoval nebo mlčel při chybě API. Nový frontend přidává výchozí stav, validaci prázdného vstupu, bezpečné zpracování ne-JSON odpovědi, čitelné chybové hlášky a samostatné renderování pro chain i Pattern Library.
