const out=document.querySelector('#exp-out'),esc=x=>String(x??'').replaceAll('&','&amp;').replaceAll('<','&lt;');document.querySelector('#exp-form').onsubmit=async e=>{e.preventDefault();let expected=document.querySelector('#exp-expected').value.split(/
/).map(x=>x.trim()).filter(Boolean),scanned=document.querySelector('#exp-scanned').value.split(/
/).map(x=>x.trim()).filter(Boolean);let r=await fetch('/api/v1/expected-audit',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({expected,scanned})}),d=await r.json();out.innerHTML=`<div class="result-chip"><strong>Nalezené</strong><br><small>${esc(d.found.join(', ')||'—')}</small></div><div class="result-chip"><strong>Chybějící</strong><br><small>${esc(d.missing.join(', ')||'—')}</small></div><div class="result-chip"><strong>Nečekané</strong><br><small>${esc(d.unexpected.join(', ')||'—')}</small></div>`};