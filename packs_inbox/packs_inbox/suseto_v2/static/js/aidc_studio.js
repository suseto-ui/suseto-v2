const form=document.getElementById('aidc-form'),out=document.getElementById('aidc-output'),dl=document.getElementById('download-code');let currentUrl=null;form.addEventListener('submit',async e=>{e.preventDefault();out.textContent='Generuji…';if(currentUrl)URL.revokeObjectURL(currentUrl);try{const r=await fetch('/api/v1/aidc/generate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({kind:document.getElementById('aidc-kind').value,data:document.getElementById('aidc-data').value,format:document.getElementById('aidc-format').value})});if(!r.ok){const x=await r.json();throw Error(x.error||'Generování selhalo')}const b=await r.blob();currentUrl=URL.createObjectURL(b);out.innerHTML='';const img=document.createElement('img');img.src=currentUrl;img.alt='Vygenerovaný optický kód';out.append(img);dl.href=currentUrl;dl.download=`suseto-code.${document.getElementById('aidc-format').value}`;dl.hidden=false;dl.textContent='Stáhnout výstup'}catch(x){out.textContent=x.message}});document.querySelectorAll('.preset').forEach(b=>b.addEventListener('click',()=>{document.getElementById('aidc-kind').value=b.dataset.kind;document.getElementById('aidc-data').value=b.dataset.value;form.requestSubmit()}));